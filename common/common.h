#ifndef __COMMON_H__
#define __COMMON_H__

#include <libemb/emb_config.h>

//////////////////////////// change this by specific chip ////////////////////////////

/**
 * chip: STC8H8K64U
 * ram: 256B idata, 8KB xdata
 * flash: 64KB
 *   - 4KB for bootloader
 *   - 60KB for application
 */

#define STC_RAM_SIZE 0x2000   // STC8H8K64U has 8KB xdata
#define STC_ROM_SIZE 0x10000  // STC8H8K64U has 64KB flash

//////////////////////////// change this by you want ////////////////////////////

// #define MAIN_Fosc 11520000UL
// #define MAIN_Fosc 12000000UL
// #define MAIN_Fosc 22118400UL
#define MAIN_Fosc 24000000UL
#define T1MS (65536 - MAIN_Fosc / 1000)  // 1T

#define DFU_TAG 0x12ABCD34UL                       // force DFU mode
#define DFU_ADDR (STC_RAM_SIZE - sizeof(DFU_TAG))  // at the last 4 bytes of RAM space

#define LDR_VERSION 0x0100  // bootloader version 1.0

//////////////////////////// shared pin configuration ////////////////////////////

// 开天斧板子P1.3被NTC测温电路占用了

#define PORT_COUNT 4
#define DEFAULT_PORT_MODE PORT_MODE_IN_HIZ

#define SHARED_GPIO_TABLES_MAP(XX)              \
    XX(2, 0, io_pup, LED_RUN)  /* 运行指示灯 */ \
    XX(2, 3, io_pup, SPI_MOSI) /* SPI MOSI */   \
    XX(2, 4, io_pup, SPI_MISO) /* SPI MISO */   \
    XX(2, 5, io_pup, SPI_SCK)  /* SPI SCK */    \
    XX(2, 2, io_pup, SPI_NSS)  /* SPI NSS */    \
    XX(3, 0, io_pup, RXD)                       \
    XX(3, 1, io_pup, TXD)

//////////////////////////// app info ////////////////////////////

typedef struct {
    uint16_t addr;
    uint16_t size;
    uint32_t crc;
} app_info_t;

//////////////////////////// FLASH partition ////////////////////////////

// 0x0000 - 0x1000 : bootloader
// 0x1000 - 0x7000 : application
// 0x7000 - 0xD000 : backup application
// 0xD000 - 0x10000 : application eeprom

#define LDR_SIZE 0x1000      // bootloader flash space = 4KB
#define APP_MAX_SIZE 0x6000  // application max flash size = 24KB

#define IAP_ADDR_MAX (STC_ROM_SIZE - LDR_SIZE)  // when iap erasing page(by bootloader or application), the `addr` must less than this value

#define APP_CRC_ADDR (LDR_SIZE - sizeof(uint32_t))
#define APP_CRC 0x5D4D15C6   // CRC32 of app binary, generated by bootloader/make.sh
#define APP_SIZE 0x000001E4  // Size of app binary, generated by bootloader/make.sh

//////////////////////////// common functions ////////////////////////////

void delay_ms(uint16_t ms);

#endif /* __COMMON_H__ */
